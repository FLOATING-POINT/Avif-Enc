#!/usr/bin/env bash
# magick-bat-webp-png.conf
# Converts WebP → PNG using ImageMagick

set -euo pipefail

if [[ $# -ne 2 ]]; then
    echo "Usage: $0 <input.webp> <output.png>" >&2
    exit 1
fi

INPUT="$1"
OUTPUT="$2"

# Simple and reliable conversion
magick "$INPUT" "$OUTPUT"

exit $?